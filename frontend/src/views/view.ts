/**
 * Score : 0/100 STATUS: ❌ FAIL | 
 * Critical Insight: The changes modify a generic interface definition and do not show progress on creating specific UI views or components for the portal.
 */

/**
 * Build customer portal UI
 * ------------------------------
 * Change to generic interface is unrelated to portal UI views, profile, request list, or request detail.
 * Adding 'age' property does not contribute to specific UI requirements.
 */

interface SomeView {
  id: number;
  name: string;
  age: number;
}